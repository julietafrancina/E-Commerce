import { makeStyles } from "@mui/styles";
export const useStyles = makeStyles({
  title: {
    fontFamily: "Nunito Sans",
    textAlign: "center",
    marginTop: 0,
    fontWeight: 700,
    fontSize: 35,
    lineHeight: 1,
    color: "#3A3333",
  },
});
